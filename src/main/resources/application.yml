
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_querydsl?serverTimezone=Asia/Seoul
    username: root
    password: yyb1emdrmq
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      #hbm2ddl.auto: update
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000 #??? ??


  logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace